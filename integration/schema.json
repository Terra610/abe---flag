{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ABE_CFF_V1",
  "description": "Constitutional Funding Forensics — CSV schema for lawful vs. unlawful federal expenditure analysis.",
  "type": "object",
  "required": [
    "program_name",
    "federal_authority_citation",
    "fiscal_year",
    "state_or_jurisdiction",
    "agency",
    "line_item_description",
    "spend_category_code",
    "amount_usd"
  ],
  "properties": {
    "program_name": {
      "type": "string",
      "description": "Name of the federal funding stream (MCSAP, Medicaid, Title I, etc.)"
    },
    "federal_authority_citation": {
      "type": "string",
      "description": "Statutory basis for the program (e.g., 49 U.S.C. 31102)."
    },
    "fiscal_year": {
      "type": ["string", "number"],
      "description": "Funding fiscal year (e.g., 2024)."
    },
    "state_or_jurisdiction": {
      "type": "string",
      "description": "State or territorial jurisdiction receiving funds."
    },
    "agency": {
      "type": "string",
      "description": "Implementing state agency receiving or using the funds."
    },
    "line_item_description": {
      "type": "string",
      "description": "Description of the funded activity."
    },
    "spend_category_code": {
      "type": "string",
      "enum": ["ON_MISSION", "OFF_MISSION", "UNCLEAR"],
      "description": "CFF classification of whether expenditures align with statutory purpose."
    },
    "amount_usd": {
      "type": ["string", "number"],
      "description": "Dollar amount attached to the line item.",
      "pattern": "^[0-9,.$\\-\\s]+$"
    },
    "notes": {
      "type": "string",
      "description": "Optional notes, audit references, GAO/OIG citations."
    }
  }
}
